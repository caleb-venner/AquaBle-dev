"""AquaBle Home Assistant integration.

This integration is a placeholder stub. The BLE device layer has moved to
the standalone AquaBle Docker container (src/aquable_container/).

Future implementation will be a thin REST client that communicates with
the container's API to expose device state and controls as HA entities.
"""
