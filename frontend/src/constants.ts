/** Timeout constants used across the frontend application.

Centralized timeout definitions to ensure consistency with backend defaults.
*/

// Command execution timeouts (seconds)
export const COMMAND_TIMEOUT_DEFAULT = 10;
export const COMMAND_TIMEOUT_AUTO_SETTINGS = 15;
